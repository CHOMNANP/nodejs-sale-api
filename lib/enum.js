const jwtSubject = {    
    api: 'api',
};

const jwtModel = {    
    admin: 'admin',
};

const saleStatsKey = {
    day: 'day',
    week: 'week',
    month: 'month',
}

module.exports = {
    jwtSubject,
    jwtModel,
    saleStatsKey
};
